# Core
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
lightgbm>=4.0
joblib>=1.3

# Explainability
shap>=0.44
lime>=0.2.0.1

# Text branch (optional but recommended — see text_branch/distilbert_branch.py)
# If torch/transformers are not installed, or the pretrained weights cannot be
# downloaded, the module automatically falls back to a TF-IDF + LogisticRegression
# text model with the same interface.
torch>=2.1
transformers>=4.38

# API layer
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.5

# Email parsing utilities used by feature_extraction.py are all in the Python
# standard library (email, re, urllib.parse) — no extra dependency needed.
